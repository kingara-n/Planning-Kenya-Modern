import { Link } from "@tanstack/react-router";

export function PortfolioDropdown() {
  return (
    <div className="flex gap-4 mb-8">
      {/* Dropdown or buttons for category filtering */}
    </div>
  );
}
